{
  "name": "@demonicattack/eslint-plugin",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "",
  "type": "module",
  "exports": "./dist/index.js",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "yarn run build:clean && yarn run check-types && tsup",
    "build:clean": "rimraf dist",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf .yarn .eslint-config-inspector",
    "dev": "yarn dlx @eslint/config-inspector --config eslint.config.ts",
    "format": "prettier --check . --cache --cache-location .yarn/cache/.prettiercache",
    "format:fix": "prettier --write . --cache --cache-location .yarn/cache/.prettiercache --log-level=warn",
    "lint": "eslint --cache --cache-location .yarn/cache/.eslintcache",
    "lint:fix": "eslint --fix --cache --cache-location .yarn/cache/.eslintcache",
    "watch": "tsup --watch"
  },
  "prettier": "./prettier.config.js",
  "dependencies": {
    "@demonicattack/shared": "workspace:^",
    "@demonicattack/typescript-plugin": "workspace:^"
  },
  "devDependencies": {
    "@demonicattack/eslint": "workspace:^",
    "@demonicattack/prettier": "workspace:^",
    "@demonicattack/typescript": "workspace:^",
    "eslint": "^9.13.0",
    "prettier": "^3.3.3",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "yarn@4.5.1"
}
